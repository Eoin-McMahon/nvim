require('glow').setup({
    style = "dark",
    width = 250,
    height = 500,
    width_ratio = 0.8, -- maximum width of the Glow window compared to the nvim window size (overrides `width`)
    height_ratio = 1.0,
})
